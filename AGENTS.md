# GongGongGo 작업 지침

## 작업 시작 전

다음 문서를 순서대로 읽는다.

1. `README.md`
2. `docs/superpowers/specs/2026-08-03-gonggonggo-modernization-design.md`
3. `docs/HANDOFF.md`

승인된 설계는 아직 구현되지 않았다. 현재 코드의 동작과 목표 구조를 혼동하지 않는다.

## 핵심 제약

- 앱은 `127.0.0.1`에만 바인딩하는 로컬 1인용 웹앱이다.
- 외부 접속, 인증, 주기적 반복 크롤링과 알림은 현재 범위가 아니다.
- 서버는 크롤링을 기다리지 않고 먼저 응답해야 한다.
- Async Playwright의 동시 작업 수는 웹에서 `1/2/4` 중 선택하고 SQLite에 저장한다.
- 크롤러는 기존 공고의 사용자 상태를 절대 덮어쓰지 않는다. 기존 행 전체를 교체하지 말고 크롤러 소유 필드만 갱신한다.
- 사용자 상태는 `검토 대기`, `지원 예정`, `지원 완료`, `제외`다.
- 원본 제목과 원본 마감 문구는 보존한다. 파싱 실패 시 원문으로 안전하게 되돌아간다.
- 기존 JSON 마이그레이션에서는 정상 날짜 공고만 가져온다. 날짜 미파싱 공고는 가져오지 않으며 JSON 원본은 수정하거나 삭제하지 않는다.
- 영구 제거한 공고는 최소 링크 차단 기록을 남겨 다시 수집되지 않게 한다.
- 기관 필터는 파싱된 기관명에, IT 직무 필터는 파싱된 직무에만 적용한다.
- 키워드는 해당 파싱 영역에서 대소문자를 무시한 부분 일치로 판정한다.
- 대상 기관 공고는 직무와 관계없이 표시한다. 일반 기관은 IT 직무가 일치할 때만 표시한다.
- 기관·직무 키워드 목록은 웹에서 편집하고 SQLite에 저장한다.
- UTF-8 한국어 텍스트를 유지한다.

## 변경 원칙

- 요청과 승인된 설계에 직접 필요한 파일만 수정한다.
- 기존 `data/job_posts.json`을 테스트 픽스처처럼 덮어쓰지 않는다.
- 동시성 동작은 추측으로 구현하지 말고 재현 가능한 테스트를 먼저 작성한다.
- 제목·날짜·필터 파서는 순수 함수로 분리하고 경계 사례를 단위 테스트한다.
- SQLite 쓰기는 짧은 트랜잭션, 연결 분리, WAL과 busy timeout을 사용한다.
- 크롤링 일부 실패가 다른 카테고리나 웹서버를 중단시키지 않게 한다.
- 구현 완료를 주장하기 전에 관련 테스트와 실제 시작 경로를 검증한다.
- 런타임 DB, 로그, `.superpowers/` 산출물은 커밋하지 않는다.

## 문서 동기화

구현 과정에서 설계가 사용자 승인으로 바뀌면 설계 문서와 `docs/HANDOFF.md`를 함께 갱신한다. 기능이 실제로 구현된 뒤에만 README의 현재 상태를 완료 상태로 변경한다.
