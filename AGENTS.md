# GongGongGo 작업 지침

- 작업 전 `docs/adr.md`와 관련 ADR을 읽는다. 설치·실행·백업은 `README.md`를 따른다.
- 일반 작업 방식은 전역 AGENTS를 따른다. 이 파일에는 프로젝트별 규칙만 둔다.
- 중요한 결정은 `docs/`의 별도 ADR에 결정과 이유만 짧게 기록하고, 파일명을 `docs/adr.md`에 추가한다. 기존 결정 변경은 사용자와 먼저 논의한다.
- 운영 `data/`, `backup/`, JSON 원본을 테스트에 사용하거나 덮어쓰지 않는다. 테스트는 임시 DB·JSON과 네트워크 없는 fake·fixture를 사용한다.
- 크롤러는 기존 공고의 사용자 상태·핀·확인 상태를 덮어쓰지 않는다. 저장소 변경 시 ADR의 필드 소유권과 트랜잭션 규칙을 확인한다.
- 동시성 변경은 재현 테스트를 먼저 작성한다. 파서는 순수 함수와 경계 사례 테스트를 유지한다.
- 한국어 텍스트는 UTF-8로 유지한다. 런타임 DB·로그·임시 산출물을 커밋하지 않는다.
- 변경에 맞는 테스트와 `git diff --check`를 실행한다. 실행 경로를 바꿨다면 임시 데이터로 실제 시작도 검증한다.

```powershell
.\.venv\Scripts\python.exe -m pytest -v
git diff --check
```
