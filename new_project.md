현재 운영 중인 공기업 채용공고 크롤링 웹서버에 "뉴스/기관소식" 기능을 추가해줘.

기존 코드를 먼저 전체적으로 분석한 뒤 구현하고, 기존 채용공고 크롤링 기능과 DB 구조, API, UI가 깨지지 않도록 해줘.

현재 프로젝트의 주요 구조는 다음과 같다.

* FastAPI 기반 로컬 웹서버
* SQLite 사용
* Playwright 기반 채용공고 크롤링
* `config.py`: URL, 기본 설정
* `crawler.py`: 현재 네이버 카페 채용공고 크롤러
* `crawl_manager.py`: 채용공고 크롤링 실행 관리
* `database.py`: DB 초기화 및 migration
* `domain.py`: 채용공고 domain model
* `repository.py`: DB 접근 계층
* `web.py`: FastAPI route 및 웹 UI
* `runtime.py`: 서버 실행
* 기존 `job_posts` 및 채용공고 관련 테이블과 로직은 그대로 유지

중요한 원칙은 기존 채용공고 기능에 뉴스 기능을 억지로 일반화해서 섞지 않는 것이다.

`job_posts`, 기존 `CrawledPost`, 기존 `CrawlManager`, 기존 채용공고 crawler 등은 채용공고 전용 구조로 유지하고, 뉴스 및 기관 보도자료는 별도 domain, crawler, repository/table 구조로 분리해라.

## 1. 기능 목적

이 기능은 뉴스 아카이브를 만드는 것이 아니다.

목적은 취업 준비를 위해 매일 확인해야 할 경제신문과 관심 공공기관의 최신 보도자료를 한 화면에서 빠르게 확인하기 위한 "임시 Inbox"다.

장기적으로 가치가 있는 기사는 내가 직접 Obsidian에 정리할 예정이므로 웹서버 DB에는 장기간 보관할 필요가 없다.

웹서버에서는 기사 또는 보도자료의 본문을 저장하거나 표시하지 않는다.

수집 대상은 기본적으로 다음 정보만으로 한다.

* 제목
* 원문 URL
* 출처
* 카테고리
* 게시일 또는 게시시각
* 최초 수집일시

제목을 클릭하면 반드시 원문 사이트를 새 탭으로 연다.

## 2. 최상위 UI 구조

기존 채용공고 화면 옆에 최상위 탭을 추가한다.

예:

`채용공고 | 뉴스/기관소식`

기존 채용공고 화면과 기능은 그대로 유지한다.

`뉴스/기관소식` 화면에서 경제신문과 기관 보도자료를 함께 관리한다.

## 3. 경제신문 수집 대상

다음 두 신문을 대상으로 한다.

* 한국경제
* 매일경제

본문은 크롤링하지 않는다.

각 사이트의 목록 페이지에서 제목과 원문 링크, 목록에서 쉽게 얻을 수 있는 경우 게시일 또는 게시시각만 수집한다.

카테고리는 사이트마다 명칭이 조금 달라도 내부에서는 다음 범주로 통일한다.

* 주요뉴스
* 정치
* 경제
* 사회
* IT
* 세계

생활, 연예, 스포츠, 영상, 클립, 사설/칼럼, 랭킹, 신문보기 등 취업 준비와 직접 관련성이 낮은 영역은 초기 버전에서 제외한다.

신문사별 원래 카테고리명이 필요한 경우 `source_category`와 내부 통합 `category`를 별도로 저장해도 된다.

## 4. 기관 보도자료 수집 대상

다음 세 기관의 공식 홈페이지에서 "보도자료" 게시판만 수집한다.

* 한국부동산원
* 신용보증기금
* 한국가스공사

공지사항, 입찰, 채용, 일반 게시판, 경영공시 등을 모두 크롤링하지 않는다.

기관 자료는 현재 단계에서는 보도자료만 대상으로 한다.

수집 정보:

* 기관명
* 보도자료 제목
* 원문 URL
* 게시일
* 최초 수집일시

본문과 첨부파일 내용은 저장하지 않는다.

한국부동산원의 경우 반복적으로 올라오는 주간아파트가격동향, 전국주택가격동향 등 정기 통계성 자료가 있다.

복잡한 AI 분류는 하지 말고 제목 패턴으로 가능하면 다음 정도만 구분한다.

* 일반 보도자료
* 정기 통계

정기 통계 구분 구현이 사이트 구조 때문에 불필요하게 복잡해진다면 첫 버전에서는 생략해도 된다.

## 5. DB 설계

기존 `job_posts`를 뉴스용으로 확장하지 않는다.

뉴스와 보도자료를 위한 별도 테이블을 만들어라.

예상 개념:

`news_items`

필요 필드는 상황에 맞게 설계하되 최소한 다음 정보를 표현할 수 있어야 한다.

* id
* item_type

  * newspaper
  * institution
* source

  * hankyung
  * mk
  * reb
  * kodit
  * kogas 등 일관된 내부 identifier
* source_name

  * 한국경제
  * 매일경제
  * 한국부동산원
  * 신용보증기금
  * 한국가스공사
* category
* source_category 필요 시
* title
* url
* published_at 또는 published_date
* discovered_at

URL은 UNIQUE 처리한다.

추적 파라미터 등 의미 없는 query parameter 때문에 동일 기사가 중복 저장되지 않도록 URL normalize를 해라.

신문의 "주요뉴스"와 "IT" 등 여러 목록에 같은 URL이 나타나도 기사 하나만 저장한다.

과도한 중복 판정 알고리즘은 만들지 말고 URL을 기본 식별자로 한다.

## 6. DB 보관 정책

뉴스 DB는 영구 아카이브가 아니라 Inbox다.

경제신문:

* 최초 수집 또는 게시 기준 7일을 넘긴 데이터는 자동 삭제

기관 보도자료:

* 30일을 넘긴 데이터는 자동 삭제

기간이 지난 데이터는 크롤링 또는 적절한 cleanup 시점에 자동으로 제거하면 된다.

별도의 복잡한 scheduler를 추가해야만 하는 구조는 피한다.

## 7. 처리 완료 기능

기사 클릭 자체를 "읽음"으로 간주하지 않는다.

기사마다 다음 기능이 필요하다.

* 원문 보기
* 처리 완료

`원문 보기`:

* 새 탭으로 원문을 연다.
* DB에는 계속 남겨둔다.

`처리 완료`:

* 내가 해당 기사에 대해 더 이상 할 일이 없다는 의미다.
* 유용한 기사는 이 버튼을 누르기 전에 내가 직접 Obsidian에 정리한다.
* 필요 없는 기사 역시 확인 후 처리 완료할 수 있다.
* 처리 완료하면 `news_items`에서 해당 항목을 즉시 삭제한다.

별도의 `읽음`, `안 읽음`, `북마크`, `보관`, `스크랩 완료` 같은 여러 상태값은 만들지 않는다.

현재 목적에서는:

* DB에 존재 = 아직 처리할 가능성이 있음
* DB에서 삭제 = 처리 끝남

이 두 상태면 충분하다.

처리 완료된 뉴스 URL을 영구 block list에 넣어서 다시는 수집하지 않는 구조는 만들지 마라.

신문 기사는 7일, 보도자료는 30일이라는 짧은 TTL이 있기 때문에 불필요한 영구 상태 관리는 피하고 싶다.

다만 같은 크롤링 실행 또는 TTL 기간 내에 동일 URL이 즉시 재수집되어 UI에 다시 나타나는 문제가 생긴다면, 가장 단순한 방법으로 해결 방안을 제안하고 구현해라. 과도한 상태 테이블은 피한다.

## 8. 뉴스 화면 기간 UI

기간 기준으로 다음 탭 또는 필터를 제공한다.

* 오늘
* 어제
* 최근 3일
* 최근 7일

기본 진입은 `오늘`.

최근 3일과 최근 7일은 오늘을 포함한 범위다.

과거 임의 날짜 조회 기능은 구현 비용이 거의 없다면 추가해도 되지만 필수 기능은 아니다.

기관 보도자료는 30일까지 보관하지만 기본 기간 UI는 위 4개를 그대로 사용해도 된다.

기관의 오래된 보도자료를 볼 필요가 있을 경우를 위해 구현이 간단하다면 `최근 30일` 필터를 기관 자료에 사용할 수 있도록 추가해도 된다.

UI가 복잡해진다면 최근 30일은 생략하고 검색 또는 별도 간단한 필터로 처리한다.

## 9. 출처 및 종류 필터

기간 필터와 별도로 다음 필터를 제공한다.

자료 종류:

* 전체
* 경제신문
* 기관 보도자료

출처:

* 전체
* 한국경제
* 매일경제
* 한국부동산원
* 신용보증기금
* 한국가스공사

경제신문 카테고리:

* 전체
* 주요뉴스
* 정치
* 경제
* 사회
* IT
* 세계

탭을 지나치게 중첩해서 UI가 복잡해지지 않게 한다.

기간은 상단 탭, 자료 종류/출처/카테고리는 필터 형태를 우선 고려한다.

## 10. 검색

현재 DB에 남아 있는 뉴스 및 보도자료의 제목을 검색할 수 있게 한다.

복잡한 전문 검색은 필요 없다.

제목에 대한 단순 문자열 검색이면 충분하다.

## 11. 뉴스 목록 UI

화려한 카드 UI보다는 많은 제목을 빠르게 훑을 수 있는 밀도 높은 목록형 UI를 선호한다.

각 항목에서 최소한 다음이 한눈에 보여야 한다.

예:

`[한국경제] [IT] 2026-08-25 10:30`
`공공부문 AI 전환 관련 기사 제목`
`[원문 보기] [처리 완료]`

기관 보도자료:

`[한국가스공사] [보도자료] 2026-08-25`
`보도자료 제목`
`[원문 보기] [처리 완료]`

모바일에서도 최소한 확인 가능한 responsive layout으로 한다.

별도의 React, Vue 등 frontend framework는 현재 필요하지 않다.

현재 프로젝트의 Jinja2/HTML/CSS/JS 구조를 그대로 활용해라.

## 12. 크롤링 구조

기존 `crawler.py`는 네이버 카페 채용공고 DOM에 특화되어 있으므로 뉴스 크롤러를 해당 파일에 억지로 추가하지 않는다.

신문사/기관별 크롤러를 서로 분리한다.

예를 들면 개념적으로:

* newspaper/hankyung
* newspaper/mk
* institution/reb
* institution/kodit
* institution/kogas

와 같이 각 출처의 DOM selector 및 파싱 규칙을 격리한다.

특정 사이트 HTML이 변경되어 하나의 크롤러가 실패해도 다른 사이트 수집은 계속되어야 한다.

예:

* 한국경제 실패
* 매일경제 성공
* 한국부동산원 성공
* 신용보증기금 실패
* 한국가스공사 성공

이라면 성공한 데이터는 정상적으로 DB에 저장한다.

## 13. requests/BeautifulSoup과 Playwright

목록 페이지가 일반 HTTP 요청과 HTML parsing만으로 충분히 수집 가능하면 `requests/httpx + BeautifulSoup` 같은 더 가벼운 방식을 우선한다.

JavaScript 렌더링 때문에 실제로 필요할 경우에만 Playwright를 사용한다.

기존 프로젝트가 Playwright를 사용한다고 해서 모든 신규 크롤러에 Playwright를 강제하지 않는다.

사이트별로 가장 단순하고 안정적인 방법을 사용한다.

## 14. 자동 크롤링

현재 서버는 startup 시 채용공고 크롤링을 실행하고 수동 실행 기능도 존재한다.

뉴스 기능에서도 지나치게 복잡한 scheduler를 만들지 않는다.

최소 요구사항:

* 서버 시작 시 뉴스/기관소식 크롤링 가능
* 뉴스 화면에서 수동 새로고침/크롤링 버튼 제공

기존 채용공고 crawl과 뉴스 crawl이 서로의 실행 상태를 방해하지 않도록 별도로 관리한다.

경제신문을 몇 분 단위로 계속 요청하는 기능은 필요 없다.

## 15. 크롤링 진행상태 및 오류

기존 채용공고 CrawlManager처럼 실행 중 여부와 출처별 성공/실패 정도는 확인할 수 있으면 좋다.

다만 기존 `CrawlManager`를 무리하게 범용화해서 복잡도를 높일 필요는 없다.

뉴스 도메인에 맞는 별도 manager를 만드는 방향을 우선 검토해라.

필요 정보:

* 크롤링 실행 중인지
* 전체 대상 수
* 완료 대상 수
* 신규 저장 수
* 출처별 오류
* 전체 실행 오류

뉴스 크롤링 한 곳의 실패 때문에 전체 서버가 오류 상태가 되어서는 안 된다.

## 16. 로깅

현재 프로젝트의 logging 체계를 그대로 활용한다.

뉴스 크롤링에서도 다음 정도를 확인할 수 있게 로그를 남긴다.

* 뉴스 크롤링 시작/종료
* 출처별 시작/성공/실패
* 수집 개수
* 신규 저장 개수
* 중복 제외 개수
* TTL 삭제 개수
* selector 또는 parsing 오류

불필요하게 항목 하나마다 INFO 로그를 남겨 로그가 과도하게 커지지는 않게 한다.

## 17. 기존 기능 보호

이 기능을 구현하면서 다음 기존 동작을 변경하거나 깨뜨리지 마라.

* 기존 채용공고 크롤링
* 기존 채용공고 필터
* 채용공고 상태 변경
* 채용공고 영구 삭제 및 deleted_links
* 기존 설정
* 기존 crawl 실행 기록
* 기존 API
* 기존 DB 데이터

DB migration이 필요하면 기존 사용자의 DB 데이터를 보존하는 방식으로 작성한다.

새 뉴스 테이블은 `CREATE TABLE IF NOT EXISTS` 또는 프로젝트의 기존 migration 방식과 일관되게 추가한다.

## 18. 구현하지 않을 기능

현재 버전에서는 다음을 구현하지 않는다.

* 기사 본문 크롤링 및 저장
* 보도자료 본문 저장
* 첨부파일 다운로드
* AI 기사 요약
* LLM 활용
* AI 중요도 평가
* 개인화 추천
* 임베딩
* Vector DB
* 자동 Obsidian 저장
* Obsidian API 연동
* 로그인
* 사용자 계정
* 뉴스 북마크
* 복잡한 읽음 상태 관리
* 이메일/메신저 알림
* 뉴스 통계/그래프
* 별도 모바일 앱
* 외부 서버 배포를 위한 추가 인프라
* React/Next.js 등 별도 frontend framework

이 기능의 핵심은 "빠르게 모으고, 훑어보고, 필요하면 원문을 읽고, 가치 있는 것은 내가 Obsidian에 정리한 뒤 처리 완료하는 것"이다.

## 19. 개발 원칙

이 프로젝트는 포트폴리오용이 아니라 실제 취업 준비 시간을 줄이기 위한 개인 도구다.

따라서 다음을 우선한다.

1. 기존 기능 안정성
2. 단순성
3. 유지보수 용이성
4. 사이트 하나의 HTML 구조 변경이 다른 크롤러에 영향을 주지 않는 구조
5. 과도한 abstraction 방지
6. 불필요한 신규 dependency 방지
7. YAGNI

기존 코드를 먼저 충분히 읽고 현재 coding style과 구조를 최대한 따른다.

기존 구조에서 문제가 없는 부분까지 대규모 refactoring하지 않는다.

## 20. 테스트

기존 테스트가 있다면 먼저 실행해서 현재 상태를 확인한다.

구현 후 최소한 다음을 검증한다.

* 기존 채용공고 테스트가 그대로 통과
* 뉴스 URL 중복 저장 방지
* 경제신문 7일 TTL cleanup
* 기관 보도자료 30일 TTL cleanup
* 처리 완료 시 DB 삭제
* 기간 필터
* 출처 필터
* 카테고리 필터
* 제목 검색
* 특정 출처 crawler 실패 시 다른 출처 결과 저장
* 기존 DB를 가진 상태에서 새 버전 실행 가능
* 크롤링 중 하나의 malformed item 때문에 해당 출처 전체가 실패하지 않도록 가능한 범위에서 처리

실제 사이트를 매 테스트마다 호출하는 테스트는 피하고 parsing 부분은 저장된 최소 HTML fixture 또는 mock으로 테스트 가능하게 구성한다.

## 21. 작업 순서

바로 코드를 무작정 수정하지 말고 먼저 현재 repository를 분석해라.

특히 다음 파일들의 역할과 의존관계를 확인한다.

* config.py
* crawler.py
* crawl_manager.py
* database.py
* domain.py
* repository.py
* web.py
* runtime.py
* templates
* static
* tests

그 다음 현재 구조를 최대한 유지하면서 뉴스 기능을 어느 파일/모듈에 추가할지 간단한 구현 계획을 먼저 제시해라.

계획에는 최소한 다음을 포함한다.

* 추가할 module/file
* DB schema
* crawler 구조
* manager 구조
* API endpoint
* UI 변경
* cleanup 정책
* test 계획

계획을 세운 뒤 구현을 진행한다.

구현 후에는 전체 테스트를 실행하고 실제 서버를 띄울 수 있는 상태인지 확인한 뒤, 변경 사항과 중요한 설계 판단을 짧게 정리해라.

사이트의 현재 HTML 구조나 URL을 실제 확인해야 한다면 임의로 selector를 추측하지 말고 실제 페이지 구조를 확인한 뒤 구현해라.

robots.txt와 사이트 이용정책을 확인하고, 개인 열람용 제목/원문 링크 수집 범위를 넘어서지 않도록 한다.
